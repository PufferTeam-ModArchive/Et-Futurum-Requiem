name: Mention upstream repo for new pull requests

on:
  pull_request:
    types: [opened]

jobs:
  comment:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/upstream_commentor.yml@master
    secrets: inherit
    with:
      upstream: 'https://github.com/Roadhog360/Et-Futurum-Requiem'
